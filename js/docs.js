/* Auto-generated by tools/build_docs.py — do not edit by hand */
window.DFIR_DOCS = {"cheatsheets": [{"id": "bash-python-snippets", "title": "Bash / Python Snippets for IR", "html": "<h1>Bash / Python Snippets for IR</h1>\n<h2>Bash</h2>\n<h3>Bulk IOC search across log files</h3>\n<pre><code class=\"language-bash\"># Search multiple log files for a list of IOCs\ngrep -Ff iocs.txt /var/log/*.log\n\n# Search recursively with filename shown\ngrep -rn -Ff iocs.txt /var/log/</code></pre>\n<h3>Extract unique IPs from a log file</h3>\n<pre><code class=\"language-bash\">grep -oE '([0-9]{1,3}\\.){3}[0-9]{1,3}' access.log | sort -u</code></pre>\n<h3>Quick timeline from multiple log sources (sort by timestamp)</h3>\n<pre><code class=\"language-bash\">cat auth.log syslog | sort -k1,3 &gt; timeline.txt</code></pre>\n<h3>Hash every file in a directory tree, output CSV</h3>\n<pre><code class=\"language-bash\">find . -type f -exec sha256sum {} \\; | awk '{print $2&quot;,&quot;$1}' &gt; hashes.csv</code></pre>\n<h3>Watch a log file live, filtered</h3>\n<pre><code class=\"language-bash\">tail -f /var/log/auth.log | grep -i &quot;failed&quot;</code></pre>\n<h2>Python</h2>\n<h3>Parse IOCs from a text blob (quick extraction during triage)</h3>\n<pre><code class=\"language-python\">import re\n\ntext = open(&quot;report.txt&quot;).read()\n\nips = re.findall(r'\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b', text)\ndomains = re.findall(r'\\b[a-zA-Z0-9.-]+\\.[a-zA-Z]{2,}\\b', text)\nhashes_sha256 = re.findall(r'\\b[a-fA-F0-9]{64}\\b', text)\nhashes_md5 = re.findall(r'\\b[a-fA-F0-9]{32}\\b', text)\n\nprint(set(ips))</code></pre>\n<h3>Bulk-check IPs against a local blocklist file</h3>\n<pre><code class=\"language-python\">blocklist = set(open(&quot;blocklist.txt&quot;).read().splitlines())\nobserved_ips = set(open(&quot;observed_ips.txt&quot;).read().splitlines())\n\nmatches = observed_ips &amp; blocklist\nprint(f&quot;Matched {len(matches)} known-bad IPs:&quot;)\nfor ip in matches:\n    print(ip)</code></pre>\n<h3>Parse Windows Event Log exports (CSV) for a specific EventID</h3>\n<pre><code class=\"language-python\">import csv\n\nwith open(&quot;security_events.csv&quot;) as f:\n    reader = csv.DictReader(f)\n    failed_logons = [row for row in reader if row.get(&quot;EventID&quot;) == &quot;4625&quot;]\n\nfrom collections import Counter\nby_account = Counter(row[&quot;TargetUserName&quot;] for row in failed_logons)\nfor account, count in by_account.most_common(10):\n    print(f&quot;{account}: {count}&quot;)</code></pre>\n<h3>Quick file entropy check (spot packed/encrypted malware)</h3>\n<pre><code class=\"language-python\">import math\nfrom collections import Counter\n\ndef entropy(data: bytes) -&gt; float:\n    if not data:\n        return 0.0\n    counts = Counter(data)\n    length = len(data)\n    return -sum((c / length) * math.log2(c / length) for c in counts.values())\n\nwith open(&quot;suspicious_file&quot;, &quot;rb&quot;) as f:\n    data = f.read()\nprint(f&quot;Entropy: {entropy(data):.2f}&quot;)  # &gt;7.5 often indicates packing/encryption</code></pre>\n<h3>Simple JSON log correlation by timestamp window</h3>\n<pre><code class=\"language-python\">import json\nfrom datetime import datetime, timedelta\n\ndef load_events(path):\n    with open(path) as f:\n        return [json.loads(line) for line in f]\n\nevents = load_events(&quot;events.jsonl&quot;)\ntarget_time = datetime.fromisoformat(&quot;2026-07-11T14:30:00&quot;)\nwindow = timedelta(minutes=10)\n\nnearby = [\n    e for e in events\n    if abs(datetime.fromisoformat(e[&quot;timestamp&quot;]) - target_time) &lt;= window\n]\nprint(f&quot;{len(nearby)} events within 10 min of target time&quot;)</code></pre>\n<h2>Notes</h2>\n<ul>\n<li>These are meant as fast-start snippets during an incident, not production tooling — adapt field names/paths to your actual log format.</li>\n<li>Keep a <code>~/ir-toolkit/</code> on any analysis box with these pre-saved so you're not retyping regex under pressure.</li>\n</ul>", "search": "# bash / python snippets for ir\n\n## bash\n\n### bulk ioc search across log files\n```bash\n# search multiple log files for a list of iocs\ngrep -ff iocs.txt /var/log/*.log\n\n# search recursively with filename shown\ngrep -rn -ff iocs.txt /var/log/\n```\n\n### extract unique ips from a log file\n```bash\ngrep -oe '([0-9]{1,3}\\.){3}[0-9]{1,3}' access.log | sort -u\n```\n\n### quick timeline from multiple log sources (sort by timestamp)\n```bash\ncat auth.log syslog | sort -k1,3 > timeline.txt\n```\n\n### hash every file in a directory tree, output csv\n```bash\nfind . -type f -exec sha256sum {} \\; | awk '{print $2\",\"$1}' > hashes.csv\n```\n\n### watch a log file live, filtered\n```bash\ntail -f /var/log/auth.log | grep -i \"failed\"\n```\n\n## python\n\n### parse iocs from a text blob (quick extraction during triage)\n```python\nimport re\n\ntext = open(\"report.txt\").read()\n\nips = re.findall(r'\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b', text)\ndomains = re.findall(r'\\b[a-za-z0-9.-]+\\.[a-za-z]{2,}\\b', text)\nhashes_sha256 = re.findall(r'\\b[a-fa-f0-9]{64}\\b', text)\nhashes_md5 = re.findall(r'\\b[a-fa-f0-9]{32}\\b', text)\n\nprint(set(ips))\n```\n\n### bulk-check ips against a local blocklist file\n```python\nblocklist = set(open(\"blocklist.txt\").read().splitlines())\nobserved_ips = set(open(\"observed_ips.txt\").read().splitlines())\n\nmatches = observed_ips & blocklist\nprint(f\"matched {len(matches)} known-bad ips:\")\nfor ip in matches:\n    print(ip)\n```\n\n### parse windows event log exports (csv) for a specific eventid\n```python\nimport csv\n\nwith open(\"security_events.csv\") as f:\n    reader = csv.dictreader(f)\n    failed_logons = [row for row in reader if row.get(\"eventid\") == \"4625\"]\n\nfrom collections import counter\nby_account = counter(row[\"targetusername\"] for row in failed_logons)\nfor account, count in by_account.most_common(10):\n    print(f\"{account}: {count}\")\n```\n\n### quick file entropy check (spot packed/encrypted malware)\n```python\nimport math\nfrom collections import counter\n\ndef entropy(data: bytes) -> float:\n    if not data:\n        return 0.0\n    counts = counter(data)\n    length = len(data)\n    return -sum((c / length) * math.log2(c / length) for c in counts.values())\n\nwith open(\"suspicious_file\", \"rb\") as f:\n    data = f.read()\nprint(f\"entropy: {entropy(data):.2f}\")  # >7.5 often indicates packing/encryption\n```\n\n### simple json log correlation by timestamp window\n```python\nimport json\nfrom datetime import datetime, timedelta\n\ndef load_events(path):\n    with open(path) as f:\n        return [json.loads(line) for line in f]\n\nevents = load_events(\"events.jsonl\")\ntarget_time = datetime.fromisoformat(\"2026-07-11t14:30:00\")\nwindow = timedelta(minutes=10)\n\nnearby = [\n    e for e in events\n    if abs(datetime.fromisoformat(e[\"timestamp\"]) - target_time) <= window\n]\nprint(f\"{len(nearby)} events within 10 min of target time\")\n```\n\n## notes\n- these are meant as fast-start snippets during an incident, not production tooling — adapt field names/paths to your actual log format.\n- keep a `~/ir-toolkit/` on any analysis box with these pre-saved so you're not retyping regex under pressure.\n"}, {"id": "kql", "title": "KQL (Kusto Query Language) Cheatsheet", "html": "<h1>KQL (Kusto Query Language) Cheatsheet</h1>\n<p>\nFor Microsoft Sentinel / Defender / Log Analytics.\n</p>\n<h2>Basics</h2>\n<pre><code class=\"language-kql\">// Basic table query with time filter\nSecurityEvent\n| where TimeGenerated &gt; ago(24h)\n| where EventID == 4625\n\n// Project specific columns\nSecurityEvent\n| project TimeGenerated, Account, Computer, EventID\n\n// Count by field\nSecurityEvent\n| summarize count() by Account\n\n// Top N\nSecurityEvent\n| summarize count() by IpAddress\n| top 10 by count_</code></pre>\n<h2>IR-relevant queries</h2>\n<h3>Failed logons by account/source</h3>\n<pre><code class=\"language-kql\">SecurityEvent\n| where EventID == 4625\n| summarize FailCount = count() by TargetAccount, IpAddress\n| where FailCount &gt; 10\n| sort by FailCount desc</code></pre>\n<h3>Impossible travel (sign-ins from geographically distant locations in short time)</h3>\n<pre><code class=\"language-kql\">SigninLogs\n| where TimeGenerated &gt; ago(1d)\n| summarize Locations = make_set(Location) by UserPrincipalName\n| where array_length(Locations) &gt; 1</code></pre>\n<h3>Suspicious PowerShell execution (Defender for Endpoint)</h3>\n<pre><code class=\"language-kql\">DeviceProcessEvents\n| where FileName =~ &quot;powershell.exe&quot;\n| where ProcessCommandLine has_any (&quot;-enc&quot;, &quot;-EncodedCommand&quot;, &quot;-nop&quot;, &quot;-w hidden&quot;)\n| project Timestamp, DeviceName, AccountName, ProcessCommandLine</code></pre>\n<h3>New/rare process across the fleet (baseline deviation)</h3>\n<pre><code class=\"language-kql\">DeviceProcessEvents\n| where Timestamp &gt; ago(7d)\n| summarize DeviceCount = dcount(DeviceName) by FileName\n| where DeviceCount == 1\n| sort by DeviceCount asc</code></pre>\n<h3>Outbound connections to rare external IPs</h3>\n<pre><code class=\"language-kql\">DeviceNetworkEvents\n| where Timestamp &gt; ago(1d)\n| where RemoteIPType == &quot;Public&quot;\n| summarize count() by RemoteIP, DeviceName\n| sort by count_ asc</code></pre>\n<h3>Lateral movement — new admin logon on multiple hosts</h3>\n<pre><code class=\"language-kql\">SecurityEvent\n| where EventID == 4672 // special privileges assigned\n| summarize HostCount = dcount(Computer) by Account\n| where HostCount &gt; 3</code></pre>\n<h3>Email-based initial access (Defender for Office 365)</h3>\n<pre><code class=\"language-kql\">EmailEvents\n| where Timestamp &gt; ago(1d)\n| where ThreatTypes has &quot;Phish&quot;\n| project Timestamp, RecipientEmailAddress, SenderFromAddress, Subject</code></pre>\n<h3>File creation matching known ransomware extensions</h3>\n<pre><code class=\"language-kql\">DeviceFileEvents\n| where FileName endswith &quot;.locked&quot; or FileName endswith &quot;.encrypted&quot;\n| summarize count() by DeviceName, bin(Timestamp, 5m)</code></pre>\n<h2>Useful operators</h2>\n<pre><code class=\"language-kql\">where | project | summarize | join | extend | parse | make_set | bin() | ago() | has / has_any / contains</code></pre>\n<ul>\n<li><code>has</code> is faster than <code>contains</code> for whole-word matches — prefer it when possible</li>\n<li><code>bin(Timestamp, 5m)</code> — bucket events into time windows, useful for spotting bursts</li>\n<li><code>join kind=inner</code> — correlate across tables (e.g. DeviceProcessEvents + DeviceNetworkEvents by DeviceId)</li>\n</ul>", "search": "# kql (kusto query language) cheatsheet\n\nfor microsoft sentinel / defender / log analytics.\n\n## basics\n```kql\n// basic table query with time filter\nsecurityevent\n| where timegenerated > ago(24h)\n| where eventid == 4625\n\n// project specific columns\nsecurityevent\n| project timegenerated, account, computer, eventid\n\n// count by field\nsecurityevent\n| summarize count() by account\n\n// top n\nsecurityevent\n| summarize count() by ipaddress\n| top 10 by count_\n```\n\n## ir-relevant queries\n\n### failed logons by account/source\n```kql\nsecurityevent\n| where eventid == 4625\n| summarize failcount = count() by targetaccount, ipaddress\n| where failcount > 10\n| sort by failcount desc\n```\n\n### impossible travel (sign-ins from geographically distant locations in short time)\n```kql\nsigninlogs\n| where timegenerated > ago(1d)\n| summarize locations = make_set(location) by userprincipalname\n| where array_length(locations) > 1\n```\n\n### suspicious powershell execution (defender for endpoint)\n```kql\ndeviceprocessevents\n| where filename =~ \"powershell.exe\"\n| where processcommandline has_any (\"-enc\", \"-encodedcommand\", \"-nop\", \"-w hidden\")\n| project timestamp, devicename, accountname, processcommandline\n```\n\n### new/rare process across the fleet (baseline deviation)\n```kql\ndeviceprocessevents\n| where timestamp > ago(7d)\n| summarize devicecount = dcount(devicename) by filename\n| where devicecount == 1\n| sort by devicecount asc\n```\n\n### outbound connections to rare external ips\n```kql\ndevicenetworkevents\n| where timestamp > ago(1d)\n| where remoteiptype == \"public\"\n| summarize count() by remoteip, devicename\n| sort by count_ asc\n```\n\n### lateral movement — new admin logon on multiple hosts\n```kql\nsecurityevent\n| where eventid == 4672 // special privileges assigned\n| summarize hostcount = dcount(computer) by account\n| where hostcount > 3\n```\n\n### email-based initial access (defender for office 365)\n```kql\nemailevents\n| where timestamp > ago(1d)\n| where threattypes has \"phish\"\n| project timestamp, recipientemailaddress, senderfromaddress, subject\n```\n\n### file creation matching known ransomware extensions\n```kql\ndevicefileevents\n| where filename endswith \".locked\" or filename endswith \".encrypted\"\n| summarize count() by devicename, bin(timestamp, 5m)\n```\n\n## useful operators\n```kql\nwhere | project | summarize | join | extend | parse | make_set | bin() | ago() | has / has_any / contains\n```\n\n- `has` is faster than `contains` for whole-word matches — prefer it when possible\n- `bin(timestamp, 5m)` — bucket events into time windows, useful for spotting bursts\n- `join kind=inner` — correlate across tables (e.g. deviceprocessevents + devicenetworkevents by deviceid)\n"}, {"id": "linux-commands", "title": "Linux Commands for IR", "html": "<h1>Linux Commands for IR</h1>\n<h2>System/process investigation</h2>\n<pre><code class=\"language-bash\"># Full process list with args\nps auxww\n\n# Process tree (spot suspicious parent-child relationships)\nps -ejH\npstree -p\n\n# What's a process actually doing / what files does it have open\nlsof -p &lt;pid&gt;\n\n# All open network connections + owning process\nlsof -i\nss -tulpn\n\n# Recently modified files (last 24h) — common after compromise\nfind / -mtime -1 -type f 2&gt;/dev/null\n\n# Recently modified files in specific high-value dirs\nfind /etc /var/www /home -mtime -1 -type f 2&gt;/dev/null\n\n# SUID binaries (privilege escalation vector — check for unexpected additions)\nfind / -perm -4000 -type f 2&gt;/dev/null</code></pre>\n<h2>User / auth investigation</h2>\n<pre><code class=\"language-bash\"># Who's logged in right now\nwho\nw\n\n# Login history\nlast -50\nlastb -50    # failed logins\n\n# Check for unexpected sudoers entries\ncat /etc/sudoers\nls -la /etc/sudoers.d/\n\n# Check for unexpected SSH keys (persistence mechanism)\ncat ~/.ssh/authorized_keys\nfind / -name &quot;authorized_keys&quot; 2&gt;/dev/null\n\n# Check bash history for suspicious commands (careful — attacker may have cleared/edited this)\ncat ~/.bash_history\nfind / -name &quot;.bash_history&quot; 2&gt;/dev/null</code></pre>\n<h2>Persistence mechanism checks</h2>\n<pre><code class=\"language-bash\"># Cron jobs (all users)\nfor u in $(cut -f1 -d: /etc/passwd); do crontab -u $u -l 2&gt;/dev/null; done\nls -la /etc/cron.d/ /etc/cron.daily/ /etc/cron.hourly/\n\n# Systemd services (check for unexpected/recently added units)\nsystemctl list-units --type=service --state=running\nfind /etc/systemd/system -mtime -7\n\n# Startup scripts\ncat /etc/rc.local\n\n# LD_PRELOAD hijacking (rootkit technique)\ncat /etc/ld.so.preload</code></pre>\n<h2>Network investigation</h2>\n<pre><code class=\"language-bash\"># Active connections with process names\nss -tulpn\n\n# ARP table (check for MITM/spoofing)\narp -a\n\n# DNS resolution config (check for hijacked resolvers)\ncat /etc/resolv.conf\n\n# Routing table\nip route</code></pre>\n<h2>File integrity / hashing</h2>\n<pre><code class=\"language-bash\"># Hash a file (for evidence/comparison against known-bad hashes)\nsha256sum suspicious_file\n\n# Hash all files in a directory recursively\nfind /path -type f -exec sha256sum {} \\; &gt; hashes.txt\n\n# Compare current state against a known baseline (if you have one)\ndiff baseline_hashes.txt hashes.txt</code></pre>\n<h2>Log locations worth checking</h2>\n<pre><code>/var/log/auth.log          # Debian/Ubuntu auth events\n/var/log/secure            # RHEL/CentOS auth events\n/var/log/syslog            # general system log\n/var/log/audit/audit.log   # auditd, if enabled\n~/.bash_history            # command history (unreliable, can be tampered)\n/var/log/apache2/          # web server logs (check for webshell access)\n/var/log/nginx/</code></pre>\n<h2>Quick safety notes</h2>\n<ul>\n<li>Prefer read-only commands early — avoid writing to the compromised host until evidence is captured.</li>\n<li><code>2&gt;/dev/null</code> suppresses permission-denied noise on wide <code>find</code> scans — useful for readability but don't lose track of what you're not seeing.</li>\n<li>If auditd is running, <code>ausearch</code>/<code>aureport</code> can be faster than raw log grep for specific event types.</li>\n</ul>", "search": "# linux commands for ir\n\n## system/process investigation\n```bash\n# full process list with args\nps auxww\n\n# process tree (spot suspicious parent-child relationships)\nps -ejh\npstree -p\n\n# what's a process actually doing / what files does it have open\nlsof -p <pid>\n\n# all open network connections + owning process\nlsof -i\nss -tulpn\n\n# recently modified files (last 24h) — common after compromise\nfind / -mtime -1 -type f 2>/dev/null\n\n# recently modified files in specific high-value dirs\nfind /etc /var/www /home -mtime -1 -type f 2>/dev/null\n\n# suid binaries (privilege escalation vector — check for unexpected additions)\nfind / -perm -4000 -type f 2>/dev/null\n```\n\n## user / auth investigation\n```bash\n# who's logged in right now\nwho\nw\n\n# login history\nlast -50\nlastb -50    # failed logins\n\n# check for unexpected sudoers entries\ncat /etc/sudoers\nls -la /etc/sudoers.d/\n\n# check for unexpected ssh keys (persistence mechanism)\ncat ~/.ssh/authorized_keys\nfind / -name \"authorized_keys\" 2>/dev/null\n\n# check bash history for suspicious commands (careful — attacker may have cleared/edited this)\ncat ~/.bash_history\nfind / -name \".bash_history\" 2>/dev/null\n```\n\n## persistence mechanism checks\n```bash\n# cron jobs (all users)\nfor u in $(cut -f1 -d: /etc/passwd); do crontab -u $u -l 2>/dev/null; done\nls -la /etc/cron.d/ /etc/cron.daily/ /etc/cron.hourly/\n\n# systemd services (check for unexpected/recently added units)\nsystemctl list-units --type=service --state=running\nfind /etc/systemd/system -mtime -7\n\n# startup scripts\ncat /etc/rc.local\n\n# ld_preload hijacking (rootkit technique)\ncat /etc/ld.so.preload\n```\n\n## network investigation\n```bash\n# active connections with process names\nss -tulpn\n\n# arp table (check for mitm/spoofing)\narp -a\n\n# dns resolution config (check for hijacked resolvers)\ncat /etc/resolv.conf\n\n# routing table\nip route\n```\n\n## file integrity / hashing\n```bash\n# hash a file (for evidence/comparison against known-bad hashes)\nsha256sum suspicious_file\n\n# hash all files in a directory recursively\nfind /path -type f -exec sha256sum {} \\; > hashes.txt\n\n# compare current state against a known baseline (if you have one)\ndiff baseline_hashes.txt hashes.txt\n```\n\n## log locations worth checking\n```\n/var/log/auth.log          # debian/ubuntu auth events\n/var/log/secure            # rhel/centos auth events\n/var/log/syslog            # general system log\n/var/log/audit/audit.log   # auditd, if enabled\n~/.bash_history            # command history (unreliable, can be tampered)\n/var/log/apache2/          # web server logs (check for webshell access)\n/var/log/nginx/\n```\n\n## quick safety notes\n- prefer read-only commands early — avoid writing to the compromised host until evidence is captured.\n- `2>/dev/null` suppresses permission-denied noise on wide `find` scans — useful for readability but don't lose track of what you're not seeing.\n- if auditd is running, `ausearch`/`aureport` can be faster than raw log grep for specific event types.\n"}, {"id": "nmap", "title": "Nmap Cheatsheet", "html": "<h1>Nmap Cheatsheet</h1>\n<h2>Basic scans</h2>\n<pre><code class=\"language-bash\"># Quick scan, top 1000 ports\nnmap 10.0.0.5\n\n# Scan specific ports\nnmap -p 22,80,443 10.0.0.5\n\n# Scan a range\nnmap -p 1-65535 10.0.0.5\n\n# Scan a subnet\nnmap 10.0.0.0/24\n\n# Ping sweep only (host discovery, no port scan)\nnmap -sn 10.0.0.0/24</code></pre>\n<h2>Scan types</h2>\n<pre><code class=\"language-bash\">-sS   # SYN scan (stealthy, default for privileged users)\n-sT   # TCP connect scan (no raw socket privileges needed)\n-sU   # UDP scan (slow, but needed for UDP services)\n-sV   # service/version detection\n-O    # OS detection\n-A    # aggressive: OS + version + script scan + traceroute</code></pre>\n<h2>Timing (relevant for IR — avoid tipping off attacker on a live host)</h2>\n<pre><code class=\"language-bash\">-T0   # paranoid (very slow, IDS evasion)\n-T1   # sneaky\n-T2   # polite\n-T3   # normal (default)\n-T4   # aggressive (typical for internal authorized scanning)\n-T5   # insane (fast, noisy)</code></pre>\n<h2>IR / incident-relevant use cases</h2>\n<h3>Confirm what's actually listening on a suspect host</h3>\n<pre><code class=\"language-bash\">nmap -sV -p- 10.0.0.5</code></pre>\n<h3>Find other hosts with the same exposed service (scope a vuln/compromise)</h3>\n<pre><code class=\"language-bash\">nmap -p 445 --open 10.0.0.0/24</code></pre>\n<h3>Check for a specific known-bad indicator (unexpected open port)</h3>\n<pre><code class=\"language-bash\">nmap -p 4444,8080,31337 10.0.0.0/24 --open</code></pre>\n<h3>Fast host discovery across a large range (find what's alive before deeper scan)</h3>\n<pre><code class=\"language-bash\">nmap -sn -T4 10.0.0.0/16</code></pre>\n<h3>Script scan for vulnerability checks (use cautiously — can be disruptive)</h3>\n<pre><code class=\"language-bash\">nmap --script vuln 10.0.0.5</code></pre>\n<h3>Detect service version to match against known exploited CVEs</h3>\n<pre><code class=\"language-bash\">nmap -sV --version-intensity 5 10.0.0.5</code></pre>\n<h2>Output formats (for feeding into other tools)</h2>\n<pre><code class=\"language-bash\">-oN scan.txt      # normal\n-oX scan.xml      # XML — parseable, feed into other tools\n-oG scan.gnmap    # greppable (legacy but still handy for quick grep/awk)\n-oA scan_basename # all formats at once</code></pre>\n<h2>Caution during live IR</h2>\n<ul>\n<li>Scanning a potentially compromised host can alert an attacker with host-based monitoring — prefer passive discovery (existing logs, EDR) when stealth matters.</li>\n<li>Aggressive scans (<code>-A</code>, <code>--script vuln</code>) can crash fragile/legacy services — know your environment before running against production.</li>\n<li>Get authorization/scope confirmation before scanning anything outside the already-approved incident scope.</li>\n</ul>", "search": "# nmap cheatsheet\n\n## basic scans\n```bash\n# quick scan, top 1000 ports\nnmap 10.0.0.5\n\n# scan specific ports\nnmap -p 22,80,443 10.0.0.5\n\n# scan a range\nnmap -p 1-65535 10.0.0.5\n\n# scan a subnet\nnmap 10.0.0.0/24\n\n# ping sweep only (host discovery, no port scan)\nnmap -sn 10.0.0.0/24\n```\n\n## scan types\n```bash\n-ss   # syn scan (stealthy, default for privileged users)\n-st   # tcp connect scan (no raw socket privileges needed)\n-su   # udp scan (slow, but needed for udp services)\n-sv   # service/version detection\n-o    # os detection\n-a    # aggressive: os + version + script scan + traceroute\n```\n\n## timing (relevant for ir — avoid tipping off attacker on a live host)\n```bash\n-t0   # paranoid (very slow, ids evasion)\n-t1   # sneaky\n-t2   # polite\n-t3   # normal (default)\n-t4   # aggressive (typical for internal authorized scanning)\n-t5   # insane (fast, noisy)\n```\n\n## ir / incident-relevant use cases\n\n### confirm what's actually listening on a suspect host\n```bash\nnmap -sv -p- 10.0.0.5\n```\n\n### find other hosts with the same exposed service (scope a vuln/compromise)\n```bash\nnmap -p 445 --open 10.0.0.0/24\n```\n\n### check for a specific known-bad indicator (unexpected open port)\n```bash\nnmap -p 4444,8080,31337 10.0.0.0/24 --open\n```\n\n### fast host discovery across a large range (find what's alive before deeper scan)\n```bash\nnmap -sn -t4 10.0.0.0/16\n```\n\n### script scan for vulnerability checks (use cautiously — can be disruptive)\n```bash\nnmap --script vuln 10.0.0.5\n```\n\n### detect service version to match against known exploited cves\n```bash\nnmap -sv --version-intensity 5 10.0.0.5\n```\n\n## output formats (for feeding into other tools)\n```bash\n-on scan.txt      # normal\n-ox scan.xml      # xml — parseable, feed into other tools\n-og scan.gnmap    # greppable (legacy but still handy for quick grep/awk)\n-oa scan_basename # all formats at once\n```\n\n## caution during live ir\n- scanning a potentially compromised host can alert an attacker with host-based monitoring — prefer passive discovery (existing logs, edr) when stealth matters.\n- aggressive scans (`-a`, `--script vuln`) can crash fragile/legacy services — know your environment before running against production.\n- get authorization/scope confirmation before scanning anything outside the already-approved incident scope.\n"}, {"id": "sigma", "title": "Sigma Rules Cheatsheet", "html": "<h1>Sigma Rules Cheatsheet</h1>\n<p>\nSigma = generic, SIEM-agnostic detection rule format. Converts to Splunk SPL, KQL, ES QL, etc via <code>sigma-cli</code> / <code>pysigma</code>.\n</p>\n<h2>Basic structure</h2>\n<pre><code class=\"language-yaml\">title: Suspicious PowerShell Encoded Command\nid: &lt;generate a uuid&gt;\nstatus: experimental\ndescription: Detects PowerShell execution with base64 encoded command parameter\nauthor: your-name\ndate: 2026/07/11\nlogsource:\n    category: process_creation\n    product: windows\ndetection:\n    selection:\n        Image|endswith: '\\powershell.exe'\n        CommandLine|contains:\n            - '-enc '\n            - '-EncodedCommand'\n    condition: selection\nlevel: medium\ntags:\n    - attack.execution\n    - attack.t1059.001</code></pre>\n<h2>Key fields</h2>\n<pre><code class=\"language-yaml\">logsource:\n    category: process_creation | network_connection | file_event | registry_event\n    product: windows | linux | macos\n    service: sysmon | security | powershell   # optional, narrows source\n\ndetection:\n    selection:\n        FieldName|endswith: 'value'      # modifiers below\n        FieldName|contains: 'value'\n    condition: selection                  # boolean logic across selections\nlevel: informational | low | medium | high | critical</code></pre>\n<h2>Common modifiers</h2>\n<pre><code>|contains       substring match\n|startswith     prefix match\n|endswith       suffix match\n|re             regex match\n|all            all listed values must match (AND instead of default OR)\n|base64offset   matches base64-encoded value at any offset (for encoded commands)</code></pre>\n<h2>Multi-condition example (parent-child process relationship)</h2>\n<pre><code class=\"language-yaml\">detection:\n    parent_office:\n        ParentImage|endswith:\n            - '\\winword.exe'\n            - '\\excel.exe'\n            - '\\outlook.exe'\n    child_suspicious:\n        Image|endswith:\n            - '\\cmd.exe'\n            - '\\powershell.exe'\n            - '\\wscript.exe'\n    condition: parent_office and child_suspicious</code></pre>\n<h2>Converting Sigma to your SIEM (sigma-cli)</h2>\n<pre><code class=\"language-bash\">pip install sigma-cli\nsigma plugin install splunk\nsigma plugin install microsoft365   # for KQL/Sentinel\n\n# Convert a single rule to Splunk SPL\nsigma convert -t splunk rule.yml\n\n# Convert to KQL (Sentinel)\nsigma convert -t microsoft365 -p sentinel_lower_case rule.yml\n\n# Convert a whole rule directory\nsigma convert -t splunk ./rules/ -o combined.spl</code></pre>\n<h2>Workflow tips</h2>\n<ul>\n<li>Pull public rules from the SigmaHQ repo (github.com/SigmaHQ/sigma) as a baseline, tune thresholds/fields to your environment rather than writing from scratch.</li>\n<li>Always test converted queries against known-good and known-bad sample data before deploying — field mappings differ per SIEM and silently produce zero-result &quot;working&quot; rules.</li>\n<li>Use <code>level</code> honestly — inflating everything to <code>high</code> trains analysts to ignore your alerts.</li>\n<li>Tag with MITRE ATT&amp;CK technique IDs (<code>attack.txxxx</code>) — makes coverage-mapping and reporting much easier later.</li>\n</ul>", "search": "# sigma rules cheatsheet\n\nsigma = generic, siem-agnostic detection rule format. converts to splunk spl, kql, es ql, etc via `sigma-cli` / `pysigma`.\n\n## basic structure\n```yaml\ntitle: suspicious powershell encoded command\nid: <generate a uuid>\nstatus: experimental\ndescription: detects powershell execution with base64 encoded command parameter\nauthor: your-name\ndate: 2026/07/11\nlogsource:\n    category: process_creation\n    product: windows\ndetection:\n    selection:\n        image|endswith: '\\powershell.exe'\n        commandline|contains:\n            - '-enc '\n            - '-encodedcommand'\n    condition: selection\nlevel: medium\ntags:\n    - attack.execution\n    - attack.t1059.001\n```\n\n## key fields\n```yaml\nlogsource:\n    category: process_creation | network_connection | file_event | registry_event\n    product: windows | linux | macos\n    service: sysmon | security | powershell   # optional, narrows source\n\ndetection:\n    selection:\n        fieldname|endswith: 'value'      # modifiers below\n        fieldname|contains: 'value'\n    condition: selection                  # boolean logic across selections\nlevel: informational | low | medium | high | critical\n```\n\n## common modifiers\n```\n|contains       substring match\n|startswith     prefix match\n|endswith       suffix match\n|re             regex match\n|all            all listed values must match (and instead of default or)\n|base64offset   matches base64-encoded value at any offset (for encoded commands)\n```\n\n## multi-condition example (parent-child process relationship)\n```yaml\ndetection:\n    parent_office:\n        parentimage|endswith:\n            - '\\winword.exe'\n            - '\\excel.exe'\n            - '\\outlook.exe'\n    child_suspicious:\n        image|endswith:\n            - '\\cmd.exe'\n            - '\\powershell.exe'\n            - '\\wscript.exe'\n    condition: parent_office and child_suspicious\n```\n\n## converting sigma to your siem (sigma-cli)\n```bash\npip install sigma-cli\nsigma plugin install splunk\nsigma plugin install microsoft365   # for kql/sentinel\n\n# convert a single rule to splunk spl\nsigma convert -t splunk rule.yml\n\n# convert to kql (sentinel)\nsigma convert -t microsoft365 -p sentinel_lower_case rule.yml\n\n# convert a whole rule directory\nsigma convert -t splunk ./rules/ -o combined.spl\n```\n\n## workflow tips\n- pull public rules from the sigmahq repo (github.com/sigmahq/sigma) as a baseline, tune thresholds/fields to your environment rather than writing from scratch.\n- always test converted queries against known-good and known-bad sample data before deploying — field mappings differ per siem and silently produce zero-result \"working\" rules.\n- use `level` honestly — inflating everything to `high` trains analysts to ignore your alerts.\n- tag with mitre att&ck technique ids (`attack.txxxx`) — makes coverage-mapping and reporting much easier later.\n"}, {"id": "splunk-spl", "title": "Splunk SPL Cheatsheet", "html": "<h1>Splunk SPL Cheatsheet</h1>\n<h2>Basics</h2>\n<pre><code class=\"language-spl\"># Basic search with time range implied by picker\nindex=main sourcetype=windows:security EventCode=4625\n\n# Search multiple indexes\nindex=main OR index=security\n\n# Stats count by field\nindex=main | stats count by src_ip\n\n# Top values\nindex=main | top limit=10 src_ip\n\n# Table specific fields\nindex=main | table _time, src_ip, dest_ip, action</code></pre>\n<h2>IR-relevant searches</h2>\n<h3>Failed logons (brute force / password spray)</h3>\n<pre><code class=\"language-spl\">index=wineventlog EventCode=4625\n| stats count by src_ip, user\n| where count &gt; 10\n| sort -count</code></pre>\n<h3>Successful logon after failures (possible successful brute force)</h3>\n<pre><code class=\"language-spl\">index=wineventlog (EventCode=4625 OR EventCode=4624)\n| transaction user src_ip maxspan=10m\n| where mvcount(EventCode) &gt; 1 AND searchmatch(&quot;EventCode=4624&quot;)</code></pre>\n<h3>New process creation with suspicious parent-child</h3>\n<pre><code class=\"language-spl\">index=sysmon EventCode=1\n| where match(ParentImage, &quot;(?i)winword\\.exe|excel\\.exe|outlook\\.exe&quot;)\n| where match(Image, &quot;(?i)cmd\\.exe|powershell\\.exe|wscript\\.exe&quot;)\n| table _time, ParentImage, Image, CommandLine, User</code></pre>\n<h3>PowerShell with encoded commands (common obfuscation/C2)</h3>\n<pre><code class=\"language-spl\">index=sysmon EventCode=1 Image=&quot;*powershell.exe&quot;\n| regex CommandLine=&quot;(?i)-enc(odedcommand)?\\s&quot;\n| table _time, Computer, User, CommandLine</code></pre>\n<h3>Outbound connections to rare/new destinations (possible beaconing)</h3>\n<pre><code class=\"language-spl\">index=firewall action=allowed\n| stats count, dc(dest_ip) as unique_dests by src_ip\n| sort -count</code></pre>\n<h3>DNS queries to suspicious TLDs or high entropy domains</h3>\n<pre><code class=\"language-spl\">index=dns\n| eval domain_length=len(query)\n| where domain_length &gt; 40\n| table _time, src_ip, query</code></pre>\n<h3>Lateral movement via admin shares</h3>\n<pre><code class=\"language-spl\">index=wineventlog EventCode=5140\n| where match(ShareName, &quot;(?i)C\\$|ADMIN\\$&quot;)\n| stats count by src_ip, user, ShareName</code></pre>\n<h3>Data staging / large file writes before exfil</h3>\n<pre><code class=\"language-spl\">index=sysmon EventCode=11\n| where match(TargetFilename, &quot;(?i)\\.zip$|\\.rar$|\\.7z$&quot;)\n| table _time, Computer, TargetFilename, Image</code></pre>\n<h2>Timeline building</h2>\n<pre><code class=\"language-spl\"># Pull all events for a specific host, sorted chronologically\nindex=* host=compromised-host-01\n| sort _time\n| table _time, index, sourcetype, _raw</code></pre>\n<h2>Useful modifiers</h2>\n<pre><code class=\"language-spl\">| eval | where | stats | timechart | transaction | rex | lookup | tstats</code></pre>\n<ul>\n<li><code>tstats</code> — much faster than <code>stats</code> for accelerated data models, use for large-scale historical hunts</li>\n<li><code>rex field=_raw &quot;regex&quot;</code> — extract fields on the fly without a defined extraction</li>\n<li><code>| transaction</code> — group related events (use sparingly, it's expensive at scale)</li>\n</ul>", "search": "# splunk spl cheatsheet\n\n## basics\n```spl\n# basic search with time range implied by picker\nindex=main sourcetype=windows:security eventcode=4625\n\n# search multiple indexes\nindex=main or index=security\n\n# stats count by field\nindex=main | stats count by src_ip\n\n# top values\nindex=main | top limit=10 src_ip\n\n# table specific fields\nindex=main | table _time, src_ip, dest_ip, action\n```\n\n## ir-relevant searches\n\n### failed logons (brute force / password spray)\n```spl\nindex=wineventlog eventcode=4625\n| stats count by src_ip, user\n| where count > 10\n| sort -count\n```\n\n### successful logon after failures (possible successful brute force)\n```spl\nindex=wineventlog (eventcode=4625 or eventcode=4624)\n| transaction user src_ip maxspan=10m\n| where mvcount(eventcode) > 1 and searchmatch(\"eventcode=4624\")\n```\n\n### new process creation with suspicious parent-child\n```spl\nindex=sysmon eventcode=1\n| where match(parentimage, \"(?i)winword\\.exe|excel\\.exe|outlook\\.exe\")\n| where match(image, \"(?i)cmd\\.exe|powershell\\.exe|wscript\\.exe\")\n| table _time, parentimage, image, commandline, user\n```\n\n### powershell with encoded commands (common obfuscation/c2)\n```spl\nindex=sysmon eventcode=1 image=\"*powershell.exe\"\n| regex commandline=\"(?i)-enc(odedcommand)?\\s\"\n| table _time, computer, user, commandline\n```\n\n### outbound connections to rare/new destinations (possible beaconing)\n```spl\nindex=firewall action=allowed\n| stats count, dc(dest_ip) as unique_dests by src_ip\n| sort -count\n```\n\n### dns queries to suspicious tlds or high entropy domains\n```spl\nindex=dns\n| eval domain_length=len(query)\n| where domain_length > 40\n| table _time, src_ip, query\n```\n\n### lateral movement via admin shares\n```spl\nindex=wineventlog eventcode=5140\n| where match(sharename, \"(?i)c\\$|admin\\$\")\n| stats count by src_ip, user, sharename\n```\n\n### data staging / large file writes before exfil\n```spl\nindex=sysmon eventcode=11\n| where match(targetfilename, \"(?i)\\.zip$|\\.rar$|\\.7z$\")\n| table _time, computer, targetfilename, image\n```\n\n## timeline building\n```spl\n# pull all events for a specific host, sorted chronologically\nindex=* host=compromised-host-01\n| sort _time\n| table _time, index, sourcetype, _raw\n```\n\n## useful modifiers\n```spl\n| eval | where | stats | timechart | transaction | rex | lookup | tstats\n```\n\n- `tstats` — much faster than `stats` for accelerated data models, use for large-scale historical hunts\n- `rex field=_raw \"regex\"` — extract fields on the fly without a defined extraction\n- `| transaction` — group related events (use sparingly, it's expensive at scale)\n"}, {"id": "tcpdump-wireshark", "title": "tcpdump / Wireshark Filters", "html": "<h1>tcpdump / Wireshark Filters</h1>\n<h2>tcpdump — capture</h2>\n<pre><code class=\"language-bash\"># Capture on interface, write to file\ntcpdump -i eth0 -w capture.pcap\n\n# Capture with snaplen (full packet, avoid truncation)\ntcpdump -i eth0 -s 0 -w capture.pcap\n\n# Capture specific host\ntcpdump -i eth0 host 10.0.0.5 -w capture.pcap\n\n# Capture specific port\ntcpdump -i eth0 port 443 -w capture.pcap\n\n# Rotate captures (100MB files, keep last 10)\ntcpdump -i eth0 -w capture.pcap -C 100 -W 10\n\n# Read without resolving hostnames (faster, less noisy)\ntcpdump -r capture.pcap -n</code></pre>\n<h2>tcpdump — common filters</h2>\n<pre><code class=\"language-bash\"># Traffic to/from a host\nhost 10.0.0.5\n\n# Traffic between two hosts\nhost 10.0.0.5 and host 10.0.0.10\n\n# Specific port\nport 445\ntcp port 3389\n\n# Exclude noisy traffic (e.g. SSH you're using to connect)\nnot port 22\n\n# SYN packets only (connection attempts / scan detection)\ntcp[tcpflags] &amp; tcp-syn != 0 and tcp[tcpflags] &amp; tcp-ack == 0\n\n# DNS traffic\nport 53\n\n# HTTP traffic (unencrypted, rare now but still seen internally)\nport 80\n\n# Traffic to a subnet\nnet 10.0.0.0/24</code></pre>\n<h2>Wireshark display filters</h2>\n<pre><code># IP address\nip.addr == 10.0.0.5\n\n# Source or destination separately\nip.src == 10.0.0.5\nip.dst == 10.0.0.5\n\n# Port\ntcp.port == 445\nudp.port == 53\n\n# HTTP requests only\nhttp.request\n\n# HTTP host header (find beaconing/C2 domains)\nhttp.host contains &quot;suspicious-domain&quot;\n\n# DNS queries\ndns.qry.name contains &quot;evil&quot;\n\n# TLS SNI (see what domain HTTPS traffic was headed to, even encrypted)\ntls.handshake.extensions_server_name contains &quot;suspicious&quot;\n\n# Find large data transfers (possible exfil)\ntcp.len &gt; 1000\n\n# SMB traffic (lateral movement / ransomware indicator)\nsmb2\n\n# Kerberos (auth issues, golden/silver ticket investigation)\nkerberos\n\n# Retransmissions (network issues or scanning)\ntcp.analysis.retransmission\n\n# Filter to a conversation\ntcp.stream eq 4</code></pre>\n<h2>Quick IR-relevant filters</h2>\n<pre><code># Beaconing pattern: repeated connections to same external IP, regular interval\nip.dst == &lt;suspect_ip&gt; and tcp.flags.syn == 1\n\n# Find all unique external IPs contacted (export via Statistics &gt; Conversations)\n\n# ARP spoofing / MITM indicator\narp.duplicate-address-detected\n\n# Unusual outbound port (possible C2 on non-standard port)\ntcp.port &gt; 1024 and tcp.flags.syn == 1 and ip.dst != 10.0.0.0/8</code></pre>\n<h2>Useful stats/tools in Wireshark</h2>\n<ul>\n<li><strong>Statistics → Conversations</strong>: quick view of top talkers, useful for spotting exfil or beaconing</li>\n<li><strong>Statistics → Protocol Hierarchy</strong>: spot unexpected protocols</li>\n<li><strong>File → Export Objects → HTTP</strong>: pull files transferred over HTTP for analysis</li>\n<li><strong>Follow → TCP Stream</strong>: reconstruct a full session</li>\n</ul>", "search": "# tcpdump / wireshark filters\n\n## tcpdump — capture\n```bash\n# capture on interface, write to file\ntcpdump -i eth0 -w capture.pcap\n\n# capture with snaplen (full packet, avoid truncation)\ntcpdump -i eth0 -s 0 -w capture.pcap\n\n# capture specific host\ntcpdump -i eth0 host 10.0.0.5 -w capture.pcap\n\n# capture specific port\ntcpdump -i eth0 port 443 -w capture.pcap\n\n# rotate captures (100mb files, keep last 10)\ntcpdump -i eth0 -w capture.pcap -c 100 -w 10\n\n# read without resolving hostnames (faster, less noisy)\ntcpdump -r capture.pcap -n\n```\n\n## tcpdump — common filters\n```bash\n# traffic to/from a host\nhost 10.0.0.5\n\n# traffic between two hosts\nhost 10.0.0.5 and host 10.0.0.10\n\n# specific port\nport 445\ntcp port 3389\n\n# exclude noisy traffic (e.g. ssh you're using to connect)\nnot port 22\n\n# syn packets only (connection attempts / scan detection)\ntcp[tcpflags] & tcp-syn != 0 and tcp[tcpflags] & tcp-ack == 0\n\n# dns traffic\nport 53\n\n# http traffic (unencrypted, rare now but still seen internally)\nport 80\n\n# traffic to a subnet\nnet 10.0.0.0/24\n```\n\n## wireshark display filters\n```\n# ip address\nip.addr == 10.0.0.5\n\n# source or destination separately\nip.src == 10.0.0.5\nip.dst == 10.0.0.5\n\n# port\ntcp.port == 445\nudp.port == 53\n\n# http requests only\nhttp.request\n\n# http host header (find beaconing/c2 domains)\nhttp.host contains \"suspicious-domain\"\n\n# dns queries\ndns.qry.name contains \"evil\"\n\n# tls sni (see what domain https traffic was headed to, even encrypted)\ntls.handshake.extensions_server_name contains \"suspicious\"\n\n# find large data transfers (possible exfil)\ntcp.len > 1000\n\n# smb traffic (lateral movement / ransomware indicator)\nsmb2\n\n# kerberos (auth issues, golden/silver ticket investigation)\nkerberos\n\n# retransmissions (network issues or scanning)\ntcp.analysis.retransmission\n\n# filter to a conversation\ntcp.stream eq 4\n```\n\n## quick ir-relevant filters\n```\n# beaconing pattern: repeated connections to same external ip, regular interval\nip.dst == <suspect_ip> and tcp.flags.syn == 1\n\n# find all unique external ips contacted (export via statistics > conversations)\n\n# arp spoofing / mitm indicator\narp.duplicate-address-detected\n\n# unusual outbound port (possible c2 on non-standard port)\ntcp.port > 1024 and tcp.flags.syn == 1 and ip.dst != 10.0.0.0/8\n```\n\n## useful stats/tools in wireshark\n- **statistics → conversations**: quick view of top talkers, useful for spotting exfil or beaconing\n- **statistics → protocol hierarchy**: spot unexpected protocols\n- **file → export objects → http**: pull files transferred over http for analysis\n- **follow → tcp stream**: reconstruct a full session\n"}, {"id": "yara", "title": "YARA Rules Cheatsheet", "html": "<h1>YARA Rules Cheatsheet</h1>\n<p>\nPattern-matching for malware/file identification — used in triage to quickly ID known families or suspicious characteristics.\n</p>\n<h2>Basic rule structure</h2>\n<pre><code class=\"language-yara\">rule Suspicious_PowerShell_Dropper\n{\n    meta:\n        author = &quot;your-name&quot;\n        date = &quot;2026-07-11&quot;\n        description = &quot;Detects common PowerShell dropper patterns&quot;\n        threat_level = &quot;medium&quot;\n\n    strings:\n        $s1 = &quot;IEX(New-Object&quot; nocase\n        $s2 = &quot;DownloadString(&quot; nocase\n        $s3 = &quot;-EncodedCommand&quot; nocase\n        $hex1 = { 4D 5A 90 00 03 00 00 00 }  // MZ header, embedded PE\n\n    condition:\n        2 of ($s1, $s2, $s3) or $hex1\n}</code></pre>\n<h2>String types</h2>\n<pre><code class=\"language-yara\">strings:\n    $text = &quot;cmd.exe&quot; ascii wide nocase   // text string, case-insensitive, both encodings\n    $hex = { E8 ?? ?? ?? ?? 5D }          // hex bytes, ?? = wildcard byte\n    $regex = /https?:\\/\\/[a-z0-9]+\\.onion/  // regex pattern</code></pre>\n<h2>Condition logic</h2>\n<pre><code class=\"language-yara\">condition:\n    all of them              // every string must match\n    any of them               // at least one\n    2 of ($s1, $s2, $s3)       // at least N of a set\n    $s1 and $s2 and not $s3\n    filesize &lt; 500KB\n    uint16(0) == 0x5A4D        // check PE magic bytes at file start</code></pre>\n<h2>Useful conditions for triage</h2>\n<pre><code class=\"language-yara\">// Match only on PE files (uses pe module)\nimport &quot;pe&quot;\n\nrule Suspicious_PE_No_Signature\n{\n    condition:\n        pe.number_of_signatures == 0 and\n        filesize &lt; 2MB and\n        pe.characteristics &amp; pe.EXECUTABLE_IMAGE\n}</code></pre>\n<pre><code class=\"language-yara\">// Detect high entropy sections (possible packing/encryption)\nimport &quot;math&quot;\n\nrule High_Entropy_Section\n{\n    condition:\n        math.entropy(0, filesize) &gt; 7.5\n}</code></pre>\n<h2>Running YARA</h2>\n<pre><code class=\"language-bash\"># Scan a single file\nyara rule.yar suspicious_file.exe\n\n# Scan a directory recursively\nyara -r rule.yar /mnt/evidence/\n\n# Scan running processes (requires privileges)\nyara -p &lt;pid&gt; rule.yar\n\n# Print matched strings, not just rule name\nyara -s rule.yar suspicious_file.exe\n\n# Use multiple rule files\nyara -r rules/*.yar /mnt/evidence/</code></pre>\n<h2>Sourcing rules quickly during an incident</h2>\n<ul>\n<li>YARA-Forge (community-curated, deduplicated ruleset): good default baseline</li>\n<li>Vendor threat intel feeds if you have them (often ship YARA rules with reports)</li>\n<li>Write a quick custom rule from unique strings/hashes found in your own triage — even a rough rule scoped to <em>this</em> incident helps you sweep other hosts fast</li>\n</ul>\n<h2>Common pitfalls</h2>\n<ul>\n<li>Overly broad strings (<code>&quot;http&quot;</code>) generate massive false positives — anchor on something more specific to the sample.</li>\n<li>Forgetting <code>nocase</code>/<code>wide</code> — malware authors mix case and Windows often stores strings as UTF-16 (wide).</li>\n<li>Not testing against known-clean files before a fleet-wide sweep — you'll page yourself with false positives.</li>\n</ul>", "search": "# yara rules cheatsheet\n\npattern-matching for malware/file identification — used in triage to quickly id known families or suspicious characteristics.\n\n## basic rule structure\n```yara\nrule suspicious_powershell_dropper\n{\n    meta:\n        author = \"your-name\"\n        date = \"2026-07-11\"\n        description = \"detects common powershell dropper patterns\"\n        threat_level = \"medium\"\n\n    strings:\n        $s1 = \"iex(new-object\" nocase\n        $s2 = \"downloadstring(\" nocase\n        $s3 = \"-encodedcommand\" nocase\n        $hex1 = { 4d 5a 90 00 03 00 00 00 }  // mz header, embedded pe\n\n    condition:\n        2 of ($s1, $s2, $s3) or $hex1\n}\n```\n\n## string types\n```yara\nstrings:\n    $text = \"cmd.exe\" ascii wide nocase   // text string, case-insensitive, both encodings\n    $hex = { e8 ?? ?? ?? ?? 5d }          // hex bytes, ?? = wildcard byte\n    $regex = /https?:\\/\\/[a-z0-9]+\\.onion/  // regex pattern\n```\n\n## condition logic\n```yara\ncondition:\n    all of them              // every string must match\n    any of them               // at least one\n    2 of ($s1, $s2, $s3)       // at least n of a set\n    $s1 and $s2 and not $s3\n    filesize < 500kb\n    uint16(0) == 0x5a4d        // check pe magic bytes at file start\n```\n\n## useful conditions for triage\n```yara\n// match only on pe files (uses pe module)\nimport \"pe\"\n\nrule suspicious_pe_no_signature\n{\n    condition:\n        pe.number_of_signatures == 0 and\n        filesize < 2mb and\n        pe.characteristics & pe.executable_image\n}\n```\n\n```yara\n// detect high entropy sections (possible packing/encryption)\nimport \"math\"\n\nrule high_entropy_section\n{\n    condition:\n        math.entropy(0, filesize) > 7.5\n}\n```\n\n## running yara\n```bash\n# scan a single file\nyara rule.yar suspicious_file.exe\n\n# scan a directory recursively\nyara -r rule.yar /mnt/evidence/\n\n# scan running processes (requires privileges)\nyara -p <pid> rule.yar\n\n# print matched strings, not just rule name\nyara -s rule.yar suspicious_file.exe\n\n# use multiple rule files\nyara -r rules/*.yar /mnt/evidence/\n```\n\n## sourcing rules quickly during an incident\n- yara-forge (community-curated, deduplicated ruleset): good default baseline\n- vendor threat intel feeds if you have them (often ship yara rules with reports)\n- write a quick custom rule from unique strings/hashes found in your own triage — even a rough rule scoped to *this* incident helps you sweep other hosts fast\n\n## common pitfalls\n- overly broad strings (`\"http\"`) generate massive false positives — anchor on something more specific to the sample.\n- forgetting `nocase`/`wide` — malware authors mix case and windows often stores strings as utf-16 (wide).\n- not testing against known-clean files before a fleet-wide sweep — you'll page yourself with false positives.\n"}], "playbooks": [{"id": "containment-decisions", "title": "Containment Decision Framework", "html": "<h1>Containment Decision Framework</h1>\n<p>\nPurpose: make containment calls quickly and defensibly, without waiting for perfect information.\n</p>\n<h2>The core tension</h2>\n<p>\nEvery containment action trades <strong>speed</strong> against <strong>evidence preservation</strong> and <strong>business continuity</strong>. Purple team instinct is to want full understanding first — in live IR that instinct will cost you. Contain on reasonable suspicion, refine later.\n</p>\n<h2>Containment tiers (cheapest/fastest → most disruptive)</h2>\n<div class=\"md-table-wrap\"><table class=\"md-table\"><thead><tr>\n<th>Tier</th>\n<th>Action</th>\n<th>When to use</th>\n<th>Evidence impact</th>\n</tr></thead><tbody>\n<tr>\n<td>1</td>\n<td>Revoke session/token</td>\n<td>Compromised credential, no host compromise confirmed</td>\n<td>None</td>\n</tr>\n<tr>\n<td>2</td>\n<td>Disable account</td>\n<td>Confirmed credential compromise</td>\n<td>None</td>\n</tr>\n<tr>\n<td>3</td>\n<td>Network isolation (EDR/VLAN)</td>\n<td>Host compromise, want to preserve for forensics</td>\n<td>Low — memory/disk intact</td>\n</tr>\n<tr>\n<td>4</td>\n<td>Block C2 at perimeter</td>\n<td>Known bad IP/domain, buys time</td>\n<td>None</td>\n</tr>\n<tr>\n<td>5</td>\n<td>Kill process / remove persistence</td>\n<td>Active malicious execution, isolation not immediately available</td>\n<td>Medium — may lose process memory</td>\n</tr>\n<tr>\n<td>6</td>\n<td>Power off</td>\n<td>Wiper malware, active data destruction in progress</td>\n<td>High — loses volatile memory, but stops the bleeding</td>\n</tr>\n<tr>\n<td>7</td>\n<td>Full rebuild</td>\n<td>Post-investigation, confirmed compromise</td>\n<td>N/A (should come after evidence capture)</td>\n</tr>\n</tbody></table></div>\n<p>\n<strong>Default to Tier 3 (network isolation) as your first move on a confirmed host compromise.</strong> It stops lateral movement and C2 without destroying the disk/memory state you'll want later. Only jump straight to power-off for active destructive activity (ransomware encryption in progress, wiper).\n</p>\n<h2>Questions to ask before you act</h2>\n<ol>\n<li>Is this host talking to anything else right now that would spread if I wait 10 more minutes?</li>\n<li>Do I have (or can I quickly get) a memory/disk capture before I take disruptive action? If not, is the risk of waiting acceptable?</li>\n<li>What's the business cost of this action, and who needs to know before I do it (not necessarily approve — but know)?</li>\n<li>Is this reversible? (Isolation is; a rebuild isn't.)</li>\n</ol>\n<h2>When you're wrong (and you will be, sometimes)</h2>\n<ul>\n<li>Document what you knew at decision time, not just the outcome. A reasonable call that turns out wrong given later info is still a reasonable call.</li>\n<li>If you over-contained (isolated something benign), the fix is fast and cheap — re-enable, apologize briefly, move on. Don't let fear of this stop you from containing quickly next time.</li>\n<li>If you under-contained (should have isolated but didn't), escalate the correction immediately rather than quietly fixing it.</li>\n</ul>\n<h2>Communicating a containment decision to non-technical stakeholders</h2>\n<p>\nTemplate: <strong>&quot;We're [action] on [system] to stop [risk]. This means [business impact] until [rough timeframe]. We'll update by [time].&quot;</strong>\n</p>\n<p>\nExample: &quot;We're isolating the finance-app-03 server from the network to stop potential spread. This means finance reporting will be unavailable for the next 1–2 hours. We'll update by 3:30pm.&quot;\n</p>\n<p>\nAvoid leading with technical justification unless asked — lead with action + impact + timeframe.\n</p>", "search": "# containment decision framework\n\npurpose: make containment calls quickly and defensibly, without waiting for perfect information.\n\n## the core tension\nevery containment action trades **speed** against **evidence preservation** and **business continuity**. purple team instinct is to want full understanding first — in live ir that instinct will cost you. contain on reasonable suspicion, refine later.\n\n## containment tiers (cheapest/fastest → most disruptive)\n\n| tier | action | when to use | evidence impact |\n|------|--------|-------------|------------------|\n| 1 | revoke session/token | compromised credential, no host compromise confirmed | none |\n| 2 | disable account | confirmed credential compromise | none |\n| 3 | network isolation (edr/vlan) | host compromise, want to preserve for forensics | low — memory/disk intact |\n| 4 | block c2 at perimeter | known bad ip/domain, buys time | none |\n| 5 | kill process / remove persistence | active malicious execution, isolation not immediately available | medium — may lose process memory |\n| 6 | power off | wiper malware, active data destruction in progress | high — loses volatile memory, but stops the bleeding |\n| 7 | full rebuild | post-investigation, confirmed compromise | n/a (should come after evidence capture) |\n\n**default to tier 3 (network isolation) as your first move on a confirmed host compromise.** it stops lateral movement and c2 without destroying the disk/memory state you'll want later. only jump straight to power-off for active destructive activity (ransomware encryption in progress, wiper).\n\n## questions to ask before you act\n1. is this host talking to anything else right now that would spread if i wait 10 more minutes?\n2. do i have (or can i quickly get) a memory/disk capture before i take disruptive action? if not, is the risk of waiting acceptable?\n3. what's the business cost of this action, and who needs to know before i do it (not necessarily approve — but know)?\n4. is this reversible? (isolation is; a rebuild isn't.)\n\n## when you're wrong (and you will be, sometimes)\n- document what you knew at decision time, not just the outcome. a reasonable call that turns out wrong given later info is still a reasonable call.\n- if you over-contained (isolated something benign), the fix is fast and cheap — re-enable, apologize briefly, move on. don't let fear of this stop you from containing quickly next time.\n- if you under-contained (should have isolated but didn't), escalate the correction immediately rather than quietly fixing it.\n\n## communicating a containment decision to non-technical stakeholders\ntemplate: **\"we're [action] on [system] to stop [risk]. this means [business impact] until [rough timeframe]. we'll update by [time].\"**\n\nexample: \"we're isolating the finance-app-03 server from the network to stop potential spread. this means finance reporting will be unavailable for the next 1–2 hours. we'll update by 3:30pm.\"\n\navoid leading with technical justification unless asked — lead with action + impact + timeframe.\n"}, {"id": "evidence-collection", "title": "Evidence Collection Checklist", "html": "<h1>Evidence Collection Checklist</h1>\n<p>\nOrder matters: volatile evidence first (it disappears), persistent evidence second.\n</p>\n<h2>Order of volatility (collect in this order when possible)</h2>\n<ol>\n<li>CPU registers, cache (rarely feasible to capture manually — noted for completeness)</li>\n<li><strong>RAM / memory</strong> — running processes, network connections, injected code, encryption keys</li>\n<li>Network state — active connections, ARP cache, routing table</li>\n<li>Running processes and services</li>\n<li>Disk (relatively stable, but still capture before rebuild)</li>\n<li>Logs on remote/centralized systems (SIEM, syslog server) — these outlive the host</li>\n<li>Archival/backup media</li>\n</ol>\n<h2>Quick capture commands (Linux host, live triage)</h2>\n<pre><code class=\"language-bash\"># Process list with full command lines\nps auxww &gt; processes.txt\n\n# Network connections\nnetstat -tulpn &gt; netstat.txt 2&gt;&amp;1 || ss -tulpn &gt; netstat.txt\n\n# Logged in users\nwho -a &gt; logged_in_users.txt\nlast -50 &gt; last_logins.txt\n\n# Loaded kernel modules (rootkit check)\nlsmod &gt; lsmod.txt\n\n# Scheduled tasks / persistence\ncrontab -l &gt; crontab_root.txt\nls -la /etc/cron.d/ /etc/cron.daily/ &gt; cron_dirs.txt\n\n# Memory capture (requires tool pre-installed, e.g. LiME or avml)\n# avml /tmp/memory.dump</code></pre>\n<h2>Quick capture commands (Windows host, live triage)</h2>\n<pre><code class=\"language-powershell\"># Process list\nGet-Process | Export-Csv processes.csv\n\n# Network connections\nGet-NetTCPConnection | Export-Csv netconns.csv\n\n# Logged in users / sessions\nquery user\n\n# Scheduled tasks\nGet-ScheduledTask | Export-Csv scheduled_tasks.csv\n\n# Prefetch / recent execution evidence — copy, don't modify\n# C:\\Windows\\Prefetch\\*.pf\n\n# Memory capture (requires tool, e.g. WinPmem)\n# winpmem.exe memory.raw</code></pre>\n<h2>Chain of custody basics</h2>\n<ul>\n<li>Record: who collected, what, when, from where, hash of the file (SHA256 minimum).</li>\n<li>Write-protect or hash source media before copying if physical access.</li>\n<li>Store originals separately from working copies — always work from copies.</li>\n<li>Log every access to the evidence after collection (who, when, why).</li>\n</ul>\n<h2>What NOT to do</h2>\n<ul>\n<li>Don't run antivirus/EDR remediation before capturing — it can delete the evidence you need.</li>\n<li>Don't reboot before memory capture if memory-resident malware is suspected — reboot clears RAM.</li>\n<li>Don't log into the compromised host with privileged/domain admin creds — you can hand the attacker a credential to escalate with.</li>\n<li>Don't skip documentation because &quot;you'll remember&quot; — you won't, three incidents from now.</li>\n</ul>", "search": "# evidence collection checklist\n\norder matters: volatile evidence first (it disappears), persistent evidence second.\n\n## order of volatility (collect in this order when possible)\n1. cpu registers, cache (rarely feasible to capture manually — noted for completeness)\n2. **ram / memory** — running processes, network connections, injected code, encryption keys\n3. network state — active connections, arp cache, routing table\n4. running processes and services\n5. disk (relatively stable, but still capture before rebuild)\n6. logs on remote/centralized systems (siem, syslog server) — these outlive the host\n7. archival/backup media\n\n## quick capture commands (linux host, live triage)\n```bash\n# process list with full command lines\nps auxww > processes.txt\n\n# network connections\nnetstat -tulpn > netstat.txt 2>&1 || ss -tulpn > netstat.txt\n\n# logged in users\nwho -a > logged_in_users.txt\nlast -50 > last_logins.txt\n\n# loaded kernel modules (rootkit check)\nlsmod > lsmod.txt\n\n# scheduled tasks / persistence\ncrontab -l > crontab_root.txt\nls -la /etc/cron.d/ /etc/cron.daily/ > cron_dirs.txt\n\n# memory capture (requires tool pre-installed, e.g. lime or avml)\n# avml /tmp/memory.dump\n```\n\n## quick capture commands (windows host, live triage)\n```powershell\n# process list\nget-process | export-csv processes.csv\n\n# network connections\nget-nettcpconnection | export-csv netconns.csv\n\n# logged in users / sessions\nquery user\n\n# scheduled tasks\nget-scheduledtask | export-csv scheduled_tasks.csv\n\n# prefetch / recent execution evidence — copy, don't modify\n# c:\\windows\\prefetch\\*.pf\n\n# memory capture (requires tool, e.g. winpmem)\n# winpmem.exe memory.raw\n```\n\n## chain of custody basics\n- record: who collected, what, when, from where, hash of the file (sha256 minimum).\n- write-protect or hash source media before copying if physical access.\n- store originals separately from working copies — always work from copies.\n- log every access to the evidence after collection (who, when, why).\n\n## what not to do\n- don't run antivirus/edr remediation before capturing — it can delete the evidence you need.\n- don't reboot before memory capture if memory-resident malware is suspected — reboot clears ram.\n- don't log into the compromised host with privileged/domain admin creds — you can hand the attacker a credential to escalate with.\n- don't skip documentation because \"you'll remember\" — you won't, three incidents from now.\n"}, {"id": "first-60-minutes", "title": "First 60 Minutes — Initial Triage", "html": "<h1>First 60 Minutes — Initial Triage</h1>\n<p>\nGoal: confirm scope and severity fast enough to make a containment call without freezing.\n</p>\n<h2>0–5 min: Confirm it's real</h2>\n<ul>\n<li>Re-check the alert/report source. False positive rate matters — don't page the room for noise.</li>\n<li>Identify: what triggered this? (EDR alert, SIEM correlation, user report, third-party notification)</li>\n<li>Note exact first-seen timestamp. This anchors your timeline for everything after.</li>\n</ul>\n<h2>5–15 min: Establish blast radius (fast, not perfect)</h2>\n<ul>\n<li>Which host(s)/account(s)/systems are confirmed affected right now?</li>\n<li>Is this contained to one asset or does it touch shared infra (AD, DNS, cloud IAM, CI/CD)?</li>\n<li>Check for lateral movement indicators: new logons on other hosts, new scheduled tasks, unusual auth from the affected identity.</li>\n<li><strong>Decision point:</strong> isolated single-host issue vs. potential multi-system compromise. This determines your next 45 minutes.</li>\n</ul>\n<h2>15–30 min: Stakeholder notification (don't skip this — it's the part IR newcomers under-invest in)</h2>\n<ul>\n<li>Notify your IC (incident commander) or manager per your escalation matrix — do this even if you're still not 100% sure it's real. Under-notifying is worse than a false alarm.</li>\n<li>Use a <strong>status, not a story</strong>: &quot;Investigating suspected [ransomware/BEC/lateral movement] on [system]. Confirmed impact: [X]. Next update in 30 min.&quot; Don't wait until you have the full picture to say something.</li>\n<li>If customer data or production is plausibly affected, loop in legal/privacy early — this is often a hard deadline trigger (regulatory notification clocks start ticking from discovery, not confirmation).</li>\n</ul>\n<h2>30–45 min: Containment decision</h2>\n<ul>\n<li>Can you isolate without destroying evidence? (network isolation &gt; shutdown, in most cases — shutdown kills volatile memory/artifacts)</li>\n<li>Weigh business impact of containment vs. risk of continued compromise. This is a judgment call — document your reasoning, don't just act.</li>\n<li>Common containment actions, cheapest/fastest first:</li>\n</ul>\n<p>\n- Disable compromised account(s)\n - Network-isolate host (EDR isolation, VLAN move, or ACL block) — preferred over pulling power\n - Revoke active sessions/tokens\n - Block C2 indicators at perimeter/EDR\n</p>\n<ul>\n<li><strong>Don't</strong> wipe or rebuild yet. That's premature and destroys evidence you may need later.</li>\n</ul>\n<h2>45–60 min: Set the next checkpoint</h2>\n<ul>\n<li>Write the timeline so far in one place (shared doc/channel), even rough notes — you will not remember exact timestamps later.</li>\n<li>Set a concrete next update time and stick to it. Silence during an incident erodes stakeholder trust faster than bad news.</li>\n<li>Decide: do you need more hands? Escalate now rather than at hour 3 when you're already stretched.</li>\n</ul>\n<h2>Common failure modes (for you specifically, based on stated gaps)</h2>\n<ul>\n<li><strong>Analysis paralysis before containment</strong>: you don't need 100% certainty to isolate a host. A wrong isolation costs an hour of user inconvenience; a missed containment can cost the whole environment.</li>\n<li><strong>Silent investigation</strong>: going quiet for an hour &quot;to get you real answers&quot; — stakeholders read silence as &quot;nobody's driving.&quot; Short frequent updates beat rare thorough ones.</li>\n<li><strong>Technically correct, contextually useless updates</strong>: &quot;Found evidence of a discovery-phase Cobalt Strike beacon&quot; means nothing to a VP. Translate: &quot;Attacker has a foothold, no evidence of data theft yet, we're cutting off their access now.&quot;</li>\n</ul>", "search": "# first 60 minutes — initial triage\n\ngoal: confirm scope and severity fast enough to make a containment call without freezing.\n\n## 0–5 min: confirm it's real\n- re-check the alert/report source. false positive rate matters — don't page the room for noise.\n- identify: what triggered this? (edr alert, siem correlation, user report, third-party notification)\n- note exact first-seen timestamp. this anchors your timeline for everything after.\n\n## 5–15 min: establish blast radius (fast, not perfect)\n- which host(s)/account(s)/systems are confirmed affected right now?\n- is this contained to one asset or does it touch shared infra (ad, dns, cloud iam, ci/cd)?\n- check for lateral movement indicators: new logons on other hosts, new scheduled tasks, unusual auth from the affected identity.\n- **decision point:** isolated single-host issue vs. potential multi-system compromise. this determines your next 45 minutes.\n\n## 15–30 min: stakeholder notification (don't skip this — it's the part ir newcomers under-invest in)\n- notify your ic (incident commander) or manager per your escalation matrix — do this even if you're still not 100% sure it's real. under-notifying is worse than a false alarm.\n- use a **status, not a story**: \"investigating suspected [ransomware/bec/lateral movement] on [system]. confirmed impact: [x]. next update in 30 min.\" don't wait until you have the full picture to say something.\n- if customer data or production is plausibly affected, loop in legal/privacy early — this is often a hard deadline trigger (regulatory notification clocks start ticking from discovery, not confirmation).\n\n## 30–45 min: containment decision\n- can you isolate without destroying evidence? (network isolation > shutdown, in most cases — shutdown kills volatile memory/artifacts)\n- weigh business impact of containment vs. risk of continued compromise. this is a judgment call — document your reasoning, don't just act.\n- common containment actions, cheapest/fastest first:\n  - disable compromised account(s)\n  - network-isolate host (edr isolation, vlan move, or acl block) — preferred over pulling power\n  - revoke active sessions/tokens\n  - block c2 indicators at perimeter/edr\n- **don't** wipe or rebuild yet. that's premature and destroys evidence you may need later.\n\n## 45–60 min: set the next checkpoint\n- write the timeline so far in one place (shared doc/channel), even rough notes — you will not remember exact timestamps later.\n- set a concrete next update time and stick to it. silence during an incident erodes stakeholder trust faster than bad news.\n- decide: do you need more hands? escalate now rather than at hour 3 when you're already stretched.\n\n## common failure modes (for you specifically, based on stated gaps)\n- **analysis paralysis before containment**: you don't need 100% certainty to isolate a host. a wrong isolation costs an hour of user inconvenience; a missed containment can cost the whole environment.\n- **silent investigation**: going quiet for an hour \"to get you real answers\" — stakeholders read silence as \"nobody's driving.\" short frequent updates beat rare thorough ones.\n- **technically correct, contextually useless updates**: \"found evidence of a discovery-phase cobalt strike beacon\" means nothing to a vp. translate: \"attacker has a foothold, no evidence of data theft yet, we're cutting off their access now.\"\n"}, {"id": "stakeholder-comms", "title": "Stakeholder Communication During Active Incidents", "html": "<h1>Stakeholder Communication During Active Incidents</h1>\n<p>\nPurpose: fast, reusable templates so comms never become the bottleneck. Written for the gap between &quot;technically accurate&quot; and &quot;understandable under stress.&quot;\n</p>\n<h2>Principles</h2>\n<ul>\n<li><strong>Cadence beats completeness.</strong> A short update every 30 min beats one perfect update every 3 hours. Commit to a cadence out loud and keep it, even if the update is &quot;no change since last update.&quot;</li>\n<li><strong>Lead with what changed, not what you did technically.</strong> Executives want: is it getting better or worse, what's the business impact, when's the next update.</li>\n<li><strong>Never say &quot;we don't know&quot; without a follow-up.</strong> Pair uncertainty with next steps: &quot;We don't yet know the scope, we're isolating [X] now and will confirm scope by [time].&quot;</li>\n<li><strong>Separate confirmed facts from working theories</strong> explicitly. &quot;Confirmed: X. Suspected but unconfirmed: Y.&quot; This prevents a hypothesis from being repeated as fact three meetings later.</li>\n</ul>\n<h2>Initial notification template</h2>\n<pre><code>Subject: [SEV-X] Investigating [brief description] — [system/service]\n\nWe are investigating [brief, plain-language description of the issue].\n\nConfirmed impact: [what's actually affected, or &quot;still being determined&quot;]\nCurrent status: [containment/investigation/monitoring]\nNext update: [specific time, not &quot;soon&quot;]\n\nPoint of contact: [you/IC name]</code></pre>\n<h2>Status update template (recurring)</h2>\n<pre><code>Update #[N] — [time]\n\nSince last update:\n- [what changed / what was done]\n\nCurrent status: [ongoing / contained / resolved]\nConfirmed impact: [current understanding]\nNext steps: [what's happening now]\nNext update: [specific time]</code></pre>\n<h2>Escalating severity mid-incident</h2>\n<p>\nDon't bury a severity increase in a routine update — call it out explicitly:\n</p>\n<pre><code>Update: escalating this to SEV-1. [One-sentence reason — new scope discovered, \ncustomer data confirmed affected, etc.] Bringing in [who]. Next update in 15 min.</code></pre>\n<h2>Talking to execs vs. talking to the technical team</h2>\n<p>\nSame facts, different altitude:\n</p>\n<ul>\n<li><strong>To execs:</strong> business impact, timeline, what you need from them (decisions, budget, external comms approval). Skip IOCs, TTPs, tool names unless asked.</li>\n<li><strong>To technical team:</strong> IOCs, affected systems, what's been tried, what you need help with (specific skills/access).</li>\n<li><strong>Rule of thumb:</strong> if you're about to say a tool name, technique name, or log source to an exec, ask &quot;does this change what they need to decide?&quot; If not, cut it.</li>\n</ul>\n<h2>Closing/all-clear template</h2>\n<pre><code>Subject: [RESOLVED] [brief description] — [system/service]\n\nSummary: [1-2 sentences, what happened and what was done]\nRoot cause: [if known; &quot;under investigation, RCA to follow&quot; if not]\nImpact: [final confirmed scope]\nCurrent status: resolved / monitoring for recurrence\n\nA full post-incident report will follow within [timeframe].</code></pre>\n<h2>Common mistakes to avoid (based on stated growth areas)</h2>\n<ul>\n<li>Going quiet while deep in technical work — set a phone timer for your update cadence if you have to.</li>\n<li>Over-hedging every statement into uselessness (&quot;it's possible that potentially X may have occurred&quot;) — say what you know plainly, flag what you don't know separately.</li>\n<li>Waiting for the IC/manager to ask for an update instead of pushing it proactively — proactive beats reactive every time in trust-building.</li>\n</ul>", "search": "# stakeholder communication during active incidents\n\npurpose: fast, reusable templates so comms never become the bottleneck. written for the gap between \"technically accurate\" and \"understandable under stress.\"\n\n## principles\n- **cadence beats completeness.** a short update every 30 min beats one perfect update every 3 hours. commit to a cadence out loud and keep it, even if the update is \"no change since last update.\"\n- **lead with what changed, not what you did technically.** executives want: is it getting better or worse, what's the business impact, when's the next update.\n- **never say \"we don't know\" without a follow-up.** pair uncertainty with next steps: \"we don't yet know the scope, we're isolating [x] now and will confirm scope by [time].\"\n- **separate confirmed facts from working theories** explicitly. \"confirmed: x. suspected but unconfirmed: y.\" this prevents a hypothesis from being repeated as fact three meetings later.\n\n## initial notification template\n```\nsubject: [sev-x] investigating [brief description] — [system/service]\n\nwe are investigating [brief, plain-language description of the issue].\n\nconfirmed impact: [what's actually affected, or \"still being determined\"]\ncurrent status: [containment/investigation/monitoring]\nnext update: [specific time, not \"soon\"]\n\npoint of contact: [you/ic name]\n```\n\n## status update template (recurring)\n```\nupdate #[n] — [time]\n\nsince last update:\n- [what changed / what was done]\n\ncurrent status: [ongoing / contained / resolved]\nconfirmed impact: [current understanding]\nnext steps: [what's happening now]\nnext update: [specific time]\n```\n\n## escalating severity mid-incident\ndon't bury a severity increase in a routine update — call it out explicitly:\n```\nupdate: escalating this to sev-1. [one-sentence reason — new scope discovered, \ncustomer data confirmed affected, etc.] bringing in [who]. next update in 15 min.\n```\n\n## talking to execs vs. talking to the technical team\nsame facts, different altitude:\n- **to execs:** business impact, timeline, what you need from them (decisions, budget, external comms approval). skip iocs, ttps, tool names unless asked.\n- **to technical team:** iocs, affected systems, what's been tried, what you need help with (specific skills/access).\n- **rule of thumb:** if you're about to say a tool name, technique name, or log source to an exec, ask \"does this change what they need to decide?\" if not, cut it.\n\n## closing/all-clear template\n```\nsubject: [resolved] [brief description] — [system/service]\n\nsummary: [1-2 sentences, what happened and what was done]\nroot cause: [if known; \"under investigation, rca to follow\" if not]\nimpact: [final confirmed scope]\ncurrent status: resolved / monitoring for recurrence\n\na full post-incident report will follow within [timeframe].\n```\n\n## common mistakes to avoid (based on stated growth areas)\n- going quiet while deep in technical work — set a phone timer for your update cadence if you have to.\n- over-hedging every statement into uselessness (\"it's possible that potentially x may have occurred\") — say what you know plainly, flag what you don't know separately.\n- waiting for the ic/manager to ask for an update instead of pushing it proactively — proactive beats reactive every time in trust-building.\n"}]};
